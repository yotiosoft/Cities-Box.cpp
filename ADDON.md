# Cities Box アドオン財政仕様

## 目的と責務境界

アドオンファイルでは、施設が提供する公共サービスカテゴリごとの月額維持費を定義できる。アドオンに記載する値は施設データであり、税収、支出、予算、処理タイミング、丸め、既定値の選択はゲームルールとしてRust側に実装する。

C++側はアドオンのフィールドを読み込み、値と定義有無をRust側へ渡してよい。C++側では収入・支出を計算せず、ゲームルール上の既定費用も選択しない。

## 月額維持費フィールド

`.adj` ファイルのトップレベルに、以下の省略可能な数値フィールドを定義できる。

| フィールド | 対象カテゴリ | 省略時のRust既定値 |
|---|---|---:|
| `monthly_maintenance_police` | `police` | 1,200 |
| `monthly_maintenance_fire` | `fire_department` | 1,400 |
| `monthly_maintenance_post` | `post_office` | 600 |
| `monthly_maintenance_education` | `education` | 1,000 |

実際のアドオンファイルに記述するカテゴリ名には、引き続き `UnitaryTools::categoryNameToCategoryID()` が受理する名前を使用する。

消防カテゴリの正しい綴りは `fire_department` である。同梱されている一部の旧アドオンで使われている `fire_depertment` も、後方互換のため `CategoryID::FireDepartment` として読み込む。新規アドオンでは正しい綴りを使用する。

記述例:

```json
{
  "Categories": ["police", "education"],
  "monthly_maintenance_police": 1800,
  "monthly_maintenance_education": 2400
}
```

## 値の選択規則

施設に割り当てられた各カテゴリについて、次の規則で維持費を選択する。

1. 対応する維持費フィールドが存在し、有効な0以上の整数なら、その値を使用する。
2. フィールドが存在しなければ、上表のカテゴリ別既定値を使用する。
3. 明示的に指定された `0` は有効であり、そのカテゴリの月額維持費が発生しないことを表す。
4. 負数、数値以外、または対応可能な整数範囲外の値は無効とし、カテゴリ別既定値へフォールバックする。
5. 施設に割り当てられていないカテゴリの維持費フィールドは無視する。

未定義と明示的な0は、C++・Rust境界を越えて区別できなければならない。そのため、0や負の番兵値で未定義を表現せず、維持費フィールドごとの定義有無フラグをブリッジで渡す。

## 支出計算

Rust側は、カテゴリごとに月次支出を独立して計算する。

```text
カテゴリ支出 =
    各固有施設のカテゴリ別維持費の合計
    × カテゴリ予算率
    ÷ 100
```

- 複数タイル施設はオブジェクトID単位で一度だけ数える。
- 複数の公共サービスカテゴリを持つ施設は、該当する各カテゴリへそれぞれ維持費を計上する。
- 中間演算はオーバーフローしないよう安全に処理する。
- 丸めは、カテゴリ別維持費合計に予算率を乗じた後、Rustの整数除算で行う。
- 予算率の範囲は現行どおり0～200%とする。

## 互換性

- 維持費フィールドを持たない既存アドオンも引き続きロードでき、Rust側のカテゴリ別既定値が適用される。
- アドオン維持費の導入によって、既存のセーブデータフィールドやセーブデータ形式は変更しない。
- アドオン維持費は、現在のマップ状態をRustへ渡す際に、インストール済みアドオンから読み取る。
- アドオンの維持費を変更すると、アドオン再ロード後の次回以降の月次支出に反映される。既存セーブファイル自体は書き換えない。

## 現在の実装状況

`CBAddon.cpp` は、維持費の値とフィールドの定義有無を別々に保持してRust側へ渡す。カテゴリ別既定値の選択、無効値からのフォールバック、予算率の適用、および支出計算はRust側で行う。

## アドオン作成

アドオン作成画面では、作者が選択した公共サービスカテゴリに対応する維持費入力欄だけを表示する。

- 空欄の場合はフィールドを省略し、Rust側の既定値を使用する。
- `0` の場合は明示的な0を書き込む。
- 負数と整数以外の入力は拒否する。
- 施設サイズ、使用タイル数、最大収容人数、従業員数、学生数による維持費の自動乗算は行わない。アドオン作者が、その施設全体の月額を指定する。
